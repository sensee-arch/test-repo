{
  "name": "todolist",
  "version": "1.0.0",
  "private": true,
  "description": "Vanilla JS TodoList with localStorage — integration tests",
  "type": "module",
  "scripts": {
    "test": "node tests/integration.mjs",
    "start": "python3 -m http.server 8080"
  },
  "devDependencies": {
    "playwright": "^1.48.0"
  }
}
