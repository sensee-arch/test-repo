{
  "name": "todo-app",
  "version": "1.0.0",
  "private": true,
  "description": "Vanilla JS Todo App with localStorage",
  "scripts": {
    "test": "jest --coverage --verbose",
    "test:watch": "jest --watch"
  },
  "jest": {
    "testEnvironment": "jsdom",
    "testMatch": [
      "**/__tests__/**/*.test.js"
    ]
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0"
  }
}
